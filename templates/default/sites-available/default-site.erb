server {
  listen   80;
  server_name  <%= node[:hostname] %>;

  access_log  <%= node[:nginx][:log_dir] %>/localhost.access.log;

  location / {
    root   <%= node.webapp.deploy_root -%>/nginx-default;
    index  index.html index.htm;
  }
}
